# Admin & Staff Portal

React + Vite frontend for the BITS admin and staff portal.

## Development

```bash
npm install
npm run dev
```

Runs at http://localhost:5173 and proxies `/api` to the backend at http://localhost:5001.
